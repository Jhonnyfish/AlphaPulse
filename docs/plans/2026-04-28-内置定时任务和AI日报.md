# AlphaPulse 内置定时任务 + DeepSeek AI 日报

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** 将每日报告生成内置到 AlphaPulse Go 后端，用 DeepSeek API 生成 AI 分析，用内置 scheduler 自动定时触发。

**Architecture:** 在 `internal/services/` 新增 `scheduler.go`（定时器）和 `deepseek.go`（LLM 客户端），在 `main.go` 启动时注册定时任务。复用现有 `ReportsHandler.DailyReportGenerate` 逻辑，增加 AI 分析层。

**Tech Stack:** Go `time.Ticker`（轻量 scheduler），DeepSeek API（OpenAI 兼容），现有 Gin + pgx 架构。

---

## Task 1: 扩展 Config 支持 DeepSeek

**Objective:** 添加 `DEEPSEEK_API_KEY` 和 `DEEPSEEK_MODEL` 配置项

**Files:**
- Modify: `/home/finn/alphapulse/internal/config/config.go`
- Modify: `/home/finn/alphapulse/.env`

**Step 1:** 在 `Config` struct 添加字段

```go
type Config struct {
    // ... existing fields ...
    DeepSeekAPIKey string
    DeepSeekModel  string
    DeepSeekBaseURL string
}
```

**Step 2:** 在 `Load()` 中读取

```go
cfg := &Config{
    // ... existing ...
    DeepSeekAPIKey:  os.Getenv("DEEPSEEK_API_KEY"),
    DeepSeekModel:   envOrDefault("DEEPSEEK_MODEL", "deepseek-chat"),
    DeepSeekBaseURL: envOrDefault("DEEPSEEK_BASE_URL", "https://api.deepseek.com/v1"),
}
```

**Step 3:** 在 `.env` 添加

```
DEEPSEEK_API_KEY=sk-xxx
DEEPSEEK_MODEL=deepseek-chat
```

**Step 4:** 验证: `cd /home/finn/alphapulse && go build ./...`

---

## Task 2: 创建 DeepSeek API 客户端

**Objective:** 实现 OpenAI 兼容的 LLM 客户端

**Files:**
- Create: `/home/finn/alphapulse/internal/services/deepseek.go`

**Step 1:** 实现客户端

```go
package services

import (
    "bytes"
    "context"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "time"
)

type DeepSeekClient struct {
    apiKey   string
    baseURL  string
    model    string
    client   *http.Client
}

type chatMessage struct {
    Role    string `json:"role"`
    Content string `json:"content"`
}

type chatRequest struct {
    Model    string        `json:"model"`
    Messages []chatMessage `json:"messages"`
    MaxTokens int          `json:"max_tokens,omitempty"`
}

type chatResponse struct {
    Choices []struct {
        Message chatMessage `json:"message"`
    } `json:"choices"`
    Error *struct {
        Message string `json:"message"`
    } `json:"error,omitempty"`
}

func NewDeepSeekClient(apiKey, baseURL, model string) *DeepSeekClient {
    if apiKey == "" {
        return nil // LLM disabled
    }
    return &DeepSeekClient{
        apiKey:  apiKey,
        baseURL: baseURL,
        model:   model,
        client:  &http.Client{Timeout: 60 * time.Second},
    }
}

// Enabled returns true if the client is configured
func (d *DeepSeekClient) Enabled() bool {
    return d != nil && d.apiKey != ""
}

// Chat sends a prompt and returns the response
func (d *DeepSeekClient) Chat(ctx context.Context, systemPrompt, userPrompt string) (string, error) {
    req := chatRequest{
        Model: d.model,
        Messages: []chatMessage{
            {Role: "system", Content: systemPrompt},
            {Role: "user", Content: userPrompt},
        },
        MaxTokens: 4096,
    }

    body, _ := json.Marshal(req)
    httpReq, _ := http.NewRequestWithContext(ctx, "POST", d.baseURL+"/chat/completions", bytes.NewReader(body))
    httpReq.Header.Set("Content-Type", "application/json")
    httpReq.Header.Set("Authorization", "Bearer "+d.apiKey)

    resp, err := d.client.Do(httpReq)
    if err != nil {
        return "", fmt.Errorf("deepseek request failed: %w", err)
    }
    defer resp.Body.Close()

    respBody, _ := io.ReadAll(resp.Body)
    var chatResp chatResponse
    if err := json.Unmarshal(respBody, &chatResp); err != nil {
        return "", fmt.Errorf("deepseek parse error: %w", err)
    }
    if chatResp.Error != nil {
        return "", fmt.Errorf("deepseek error: %s", chatResp.Error.Message)
    }
    if len(chatResp.Choices) == 0 {
        return "", fmt.Errorf("deepseek: empty response")
    }
    return chatResp.Choices[0].Message.Content, nil
}
```

**Step 2:** 验证: `cd /home/finn/alphapulse && go build ./...`

---

## Task 3: 创建 Scheduler 服务

**Objective:** 实现轻量级定时任务调度器

**Files:**
- Create: `/home/finn/alphapulse/internal/services/scheduler.go`

**Step 1:** 实现 scheduler

```go
package services

import (
    "log"
    "sync"
    "time"
)

type Job struct {
    Name     string
    Schedule string // cron expression (simplified: only supports interval-based)
    Fn       func()
    stop     chan struct{}
}

type Scheduler struct {
    jobs []*Job
    mu   sync.Mutex
    log  *log.Logger
}

func NewScheduler() *Scheduler {
    return &Scheduler{}
}

// AddDailyJob registers a job that runs at a specific hour:minute each day (server local time)
func (s *Scheduler) AddDailyJob(name string, hour, minute int, fn func()) {
    j := &Job{
        Name: name,
        Fn:   fn,
        stop: make(chan struct{}),
    }
    s.jobs = append(s.jobs, j)
    go s.runDaily(j, hour, minute)
}

func (s *Scheduler) runDaily(j *Job, hour, minute int) {
    for {
        now := time.Now()
        next := time.Date(now.Year(), now.Month(), now.Day(), hour, minute, 0, 0, now.Location())
        if next.Before(now) {
            next = next.Add(24 * time.Hour)
        }
        log.Printf("[scheduler] %s next run at %s", j.Name, next.Format("2006-01-02 15:04:05"))

        select {
        case <-time.After(time.Until(next)):
            log.Printf("[scheduler] running %s", j.Name)
            func() {
                defer func() {
                    if r := recover(); r != nil {
                        log.Printf("[scheduler] %s panic: %v", j.Name, r)
                    }
                }()
                j.Fn()
            }()
        case <-j.stop:
            return
        }
    }
}

// StopAll stops all scheduled jobs
func (s *Scheduler) StopAll() {
    s.mu.Lock()
    defer s.mu.Unlock()
    for _, j := range s.jobs {
        close(j.stop)
    }
}
```

**Step 2:** 验证: `go build ./...`

---

## Task 4: 增强日报生成支持 AI 分析

**Objective:** 给 ReportsHandler 注入 DeepSeek client，生成时调用 AI

**Files:**
- Modify: `/home/finn/alphapulse/internal/handlers/reports.go`

**Step 1:** 给 ReportsHandler 添加 deepseek 字段

```go
type ReportsHandler struct {
    // ... existing fields ...
    deepseek *services.DeepSeekClient
}
```

**Step 2:** 修改 NewReportsHandler 接收 deepseek 参数

```go
func NewReportsHandler(
    db *pgxpool.Pool,
    tencent *services.TencentService,
    eastMoney *services.EastMoneyService,
    analyze *AnalyzeHandler,
    watchlist *WatchlistHandler,
    log *zap.Logger,
    deepseek *services.DeepSeekClient,
) *ReportsHandler {
    // ... existing ...
    return &ReportsHandler{
        // ... existing ...
        deepseek: deepseek,
    }
}
```

**Step 3:** 在 DailyReportGenerate 的 markdown 生成后、写文件前，添加 AI 分析段

```go
// After generating the basic table, add AI analysis if available
if h.deepseek != nil && h.deepseek.Enabled() {
    aiPrompt := fmt.Sprintf("以下是今日自选股行情数据，请用中文写一段简短的市场分析（200字以内），包括整体趋势、重点关注的个股和风险提示：\n\n%s", mdContent)
    aiAnalysis, aiErr := h.deepseek.Chat(ctx, "你是专业的A股分析师，用简洁专业的语言分析股票数据。", aiPrompt)
    if aiErr == nil {
        lines = append(lines, "")
        lines = append(lines, "## 🤖 AI 分析")
        lines = append(lines, "")
        lines = append(lines, aiAnalysis)
    } else {
        log.Warn("daily report: deepseek analysis failed", zap.Error(aiErr))
    }
}
```

**Step 4:** 验证: `go build ./...`

---

## Task 5: 在 main.go 中连接 Scheduler + DeepSeek + ReportsHandler

**Objective:** 启动时注册定时任务

**Files:**
- Modify: `/home/finn/alphapulse/cmd/server/main.go`

**Step 1:** 创建 DeepSeek 客户端

```go
deepseekClient := services.NewDeepSeekClient(cfg.DeepSeekAPIKey, cfg.DeepSeekBaseURL, cfg.DeepSeekModel)
```

**Step 2:** 传递给 NewReportsHandler

```go
reportsHandler := handlers.NewReportsHandler(db, tencentService, eastMoneyService, analyzeHandler, watchlistHandler, logger.L(), deepseekClient)
```

**Step 3:** 创建 scheduler 并注册每日 15:30 自动生成

```go
scheduler := services.NewScheduler()
scheduler.AddDailyJob("daily-report", 15, 30, func() {
    log.Println("[scheduler] generating daily report...")
    reportsHandler.GenerateDailyReportAuto()
})
defer scheduler.StopAll()
```

**Step 4:** 在 ReportsHandler 添加非 HTTP 的自动生成方法

```go
// GenerateDailyReportAuto is called by the scheduler (no HTTP context)
func (h *ReportsHandler) GenerateDailyReportAuto() {
    ctx, cancel := context.WithTimeout(context.Background(), 2*time.Minute)
    defer cancel()
    // ... same logic as DailyReportGenerate but without gin.Context ...
    // Reuse the core analysis + markdown generation + file write
}
```

**Step 5:** 验证

```bash
cd /home/finn/alphapulse && go build -o bin/server ./cmd/server
# Check logs show: [scheduler] daily-report next run at ...
```

---

## Task 6: 添加定时任务管理 API（可选）

**Objective:** 允许通过 API 查看/管理定时任务状态

**Files:**
- Modify: `/home/finn/alphapulse/cmd/server/main.go` (add routes)
- Modify: `/home/finn/alphapulse/internal/services/scheduler.go` (add status method)

**Step 1:** Scheduler 添加状态方法

```go
type JobStatus struct {
    Name     string    `json:"name"`
    NextRun  time.Time `json:"next_run"`
    LastRun  time.Time `json:"last_run,omitempty"`
    LastErr  string    `json:"last_error,omitempty"`
}

func (s *Scheduler) Status() []JobStatus { ... }
```

**Step 2:** 添加 API 路由

```go
api.GET("/system/scheduler", authMiddleware, func(c *gin.Context) {
    c.JSON(200, gin.H{"jobs": scheduler.Status()})
})
```

**Step 3:** 验证: `curl -s http://localhost:8899/api/system/scheduler -H "Authorization: Bearer $TOKEN"`

---

## Task 7: 重新构建并测试

**Objective:** 编译部署，验证自动日报生成

**Step 1:** 重新构建

```bash
cd /home/finn/alphapulse
go build -o bin/server ./cmd/server
```

**Step 2:** 重启服务

```bash
# Kill old process
pkill -f "alphapulse.*server" || true
sleep 1
# Start new
./bin/server &
```

**Step 3:** 验证 scheduler 日志

```bash
# Should see in logs:
# [scheduler] daily-report next run at 2026-04-28 15:30:00
```

**Step 4:** 手动触发测试

```bash
TOKEN=$(curl -s http://localhost:8899/api/auth/login -d '{"username":"admin","password":"admin123"}' -H 'Content-Type: application/json' | python3 -c 'import json,sys; print(json.load(sys.stdin)["token"])')
curl -s -X POST http://localhost:8899/api/daily-report/generate -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

**Step 5:** 确认 AI 分析存在

```bash
# Check report contains "## 🤖 AI 分析" section
cat /home/finn/.uzi-skill/reports/daily_report_$(date +%Y%m%d).md | grep "AI 分析"
```

---

## Summary of Files Changed

| Action | File |
|--------|------|
| Modify | `internal/config/config.go` |
| Create | `internal/services/deepseek.go` |
| Create | `internal/services/scheduler.go` |
| Modify | `internal/handlers/reports.go` |
| Modify | `cmd/server/main.go` |
| Modify | `.env` |
